name: Generate voice languages data

on:
  workflow_dispatch:
  schedule:
    - cron: '0 06 * * 4'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  generate:
    name: Generate
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout the repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up Python 3.14
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.14"

      - name: Generate data
        env:
          GITHUB_GENERATOR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run generate:voice-languages

      - name: Upload data
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: voiceLanguages
          path: |
            ./_data/sttLanguages.json
            ./_data/ttsLanguages.json
          if-no-files-found: error
          retention-days: 1

  create-pr:
    name: Create PR
    runs-on: ubuntu-latest
    needs: generate
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout the repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Download data
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: "voiceLanguages"
          path: "./_data/"

      - name: Create PR
        id: pr
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "Update voice languages data"
          author: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          committer: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          sign-commits: true
          title: "Update voice languages data"
          body: |
            This PR was automatically generated by the workflow.
            It updates the voice languages data.
          base: main
          branch: update-voice-languages-data
          add-paths: |
            _data/ttsLanguages.json
            _data/sttLanguages.json